[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "muscles-mcp"
version = "1.0.0"
description = "MCP adapter for Muscles application contract"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = ["muscles>=1.0.0rc1,<2.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
